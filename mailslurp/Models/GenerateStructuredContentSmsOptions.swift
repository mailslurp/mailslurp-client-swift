//
// GenerateStructuredContentSmsOptions.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Options for generating structured content output from an SMS */
@objcMembers public class GenerateStructuredContentSmsOptions: NSObject, Codable, ParameterConvertible {

    /** SMS ID to read and pass to AI */
    public var smsId: UUID
    /** Optional instructions for the AI to follow. Try to be precise and clear. You can include examples and hints. */
    public var instructions: String?
    public var outputSchema: StructuredOutputSchema?
    /** ID of transformer to apply */
    public var transformId: UUID?

    public init(smsId: UUID, instructions: String? = nil, outputSchema: StructuredOutputSchema? = nil, transformId: UUID? = nil) {
        self.smsId = smsId
        self.instructions = instructions
        self.outputSchema = outputSchema
        self.transformId = transformId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case smsId
        case instructions
        case outputSchema
        case transformId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(smsId, forKey: .smsId)
        try container.encodeIfPresent(instructions, forKey: .instructions)
        try container.encodeIfPresent(outputSchema, forKey: .outputSchema)
        try container.encodeIfPresent(transformId, forKey: .transformId)
    }
}

