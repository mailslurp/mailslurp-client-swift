//
// PhoneMessageThreadItemProjection.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

@objcMembers public class PhoneMessageThreadItemProjection: NSObject, Codable, ParameterConvertible {

    public enum MessageDirection: String, Sendable, Codable, CaseIterable {
        case outbound = "OUTBOUND"
        case inbound = "INBOUND"
    }
    public var body: String
    public var phoneNumberId: UUID
    public var created: Date
    public var messageDirection: MessageDirection
    public var toPhoneNumber: String
    public var fromPhoneNumber: String

    public init(body: String, phoneNumberId: UUID, created: Date, messageDirection: MessageDirection, toPhoneNumber: String, fromPhoneNumber: String) {
        self.body = body
        self.phoneNumberId = phoneNumberId
        self.created = created
        self.messageDirection = messageDirection
        self.toPhoneNumber = toPhoneNumber
        self.fromPhoneNumber = fromPhoneNumber
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case body
        case phoneNumberId
        case created
        case messageDirection
        case toPhoneNumber
        case fromPhoneNumber
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(body, forKey: .body)
        try container.encode(phoneNumberId, forKey: .phoneNumberId)
        try container.encode(created, forKey: .created)
        try container.encode(messageDirection, forKey: .messageDirection)
        try container.encode(toPhoneNumber, forKey: .toPhoneNumber)
        try container.encode(fromPhoneNumber, forKey: .fromPhoneNumber)
    }
}

