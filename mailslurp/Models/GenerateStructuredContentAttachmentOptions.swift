//
// GenerateStructuredContentAttachmentOptions.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Options for generating structured content output from an attachment */
@objcMembers public class GenerateStructuredContentAttachmentOptions: NSObject, Codable, JSONEncodable {

    /** Attachment ID to read and pass to AI */
    public var attachmentId: String
    /** Optional instructions for the AI to follow. Try to be precise and clear. You can include examples and hints. */
    public var instructions: String?
    public var outputSchema: StructuredOutputSchema?
    /** ID of transformer to apply */
    public var transformId: UUID?
    /** Optional email ID for more context */
    public var emailId: UUID?

    public init(attachmentId: String, instructions: String? = nil, outputSchema: StructuredOutputSchema? = nil, transformId: UUID? = nil, emailId: UUID? = nil) {
        self.attachmentId = attachmentId
        self.instructions = instructions
        self.outputSchema = outputSchema
        self.transformId = transformId
        self.emailId = emailId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case attachmentId
        case instructions
        case outputSchema
        case transformId
        case emailId
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(attachmentId, forKey: .attachmentId)
        try container.encodeIfPresent(instructions, forKey: .instructions)
        try container.encodeIfPresent(outputSchema, forKey: .outputSchema)
        try container.encodeIfPresent(transformId, forKey: .transformId)
        try container.encodeIfPresent(emailId, forKey: .emailId)
    }
}

