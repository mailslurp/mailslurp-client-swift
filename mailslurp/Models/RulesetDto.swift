//
// RulesetDto.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Rules for an inbox or phone number. Rulesets can be used to block, allow, filter, or bounce emails or SMS when sending or receiving. */
@objcMembers public class RulesetDto: NSObject, Codable, ParameterConvertible {

    public enum Scope: String, Sendable, Codable, CaseIterable {
        case receivingEmails = "RECEIVING_EMAILS"
        case sendingEmails = "SENDING_EMAILS"
        case receivingSms = "RECEIVING_SMS"
        case sendingSms = "SENDING_SMS"
    }
    public enum Action: String, Sendable, Codable, CaseIterable {
        case block = "BLOCK"
        case allow = "ALLOW"
        case filterRemove = "FILTER_REMOVE"
        case bounceSoft = "BOUNCE_SOFT"
        case bounceHard = "BOUNCE_HARD"
    }
    public enum Handler: String, Sendable, Codable, CaseIterable {
        case exception = "EXCEPTION"
    }
    public var _id: UUID
    public var inboxId: UUID?
    public var phoneId: UUID?
    public var scope: Scope
    public var action: Action
    public var target: String
    public var handler: Handler
    public var createdAt: Date

    public init(_id: UUID, inboxId: UUID? = nil, phoneId: UUID? = nil, scope: Scope, action: Action, target: String, handler: Handler, createdAt: Date) {
        self._id = _id
        self.inboxId = inboxId
        self.phoneId = phoneId
        self.scope = scope
        self.action = action
        self.target = target
        self.handler = handler
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case _id = "id"
        case inboxId
        case phoneId
        case scope
        case action
        case target
        case handler
        case createdAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(_id, forKey: ._id)
        try container.encodeIfPresent(inboxId, forKey: .inboxId)
        try container.encodeIfPresent(phoneId, forKey: .phoneId)
        try container.encode(scope, forKey: .scope)
        try container.encode(action, forKey: .action)
        try container.encode(target, forKey: .target)
        try container.encode(handler, forKey: .handler)
        try container.encode(createdAt, forKey: .createdAt)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension RulesetDto: Identifiable {}
