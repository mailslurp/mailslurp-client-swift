//
// InboxControllerAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import PromiseKit

@objc open class InboxControllerAPI : NSObject {
    /**
     * enum for parameter inboxType
     */
    public enum InboxType_createInbox: String, CaseIterable {
        case httpInbox = "HTTP_INBOX"
        case smtpInbox = "SMTP_INBOX"
    }

    /**
     Create an inbox email address. An inbox has a real email address and can send and receive emails. Inboxes can be either `SMTP` or `HTTP` inboxes.
     
     - parameter emailAddress: (query) A custom email address to use with the inbox. Defaults to null. When null MailSlurp will assign a random email address to the inbox such as &#x60;123@mailslurp.com&#x60;. If you use the &#x60;useDomainPool&#x60; option when the email address is null it will generate an email address with a more varied domain ending such as &#x60;123@mailslurp.info&#x60; or &#x60;123@mailslurp.biz&#x60;. When a custom email address is provided the address is split into a domain and the domain is queried against your user. If you have created the domain in the MailSlurp dashboard and verified it you can use any email address that ends with the domain. Note domain types must match the inbox type - so &#x60;SMTP&#x60; inboxes will only work with &#x60;SMTP&#x60; type domains. Avoid &#x60;SMTP&#x60; inboxes if you need to send emails as they can only receive. Send an email to this address and the inbox will receive and store it for you. To retrieve the email use the Inbox and Email Controller endpoints with the inbox ID. (optional)
     - parameter tags: (query) Tags that inbox has been tagged with. Tags can be added to inboxes to group different inboxes within an account. You can also search for inboxes by tag in the dashboard UI. (optional)
     - parameter name: (query) Optional name of the inbox. Displayed in the dashboard for easier search and used as the sender name when sending emails. (optional)
     - parameter _description: (query) Optional description of the inbox for labelling purposes. Is shown in the dashboard and can be used with (optional)
     - parameter useDomainPool: (query) Use the MailSlurp domain name pool with this inbox when creating the email address. Defaults to null. If enabled the inbox will be an email address with a domain randomly chosen from a list of the MailSlurp domains. This is useful when the default &#x60;@mailslurp.com&#x60; email addresses used with inboxes are blocked or considered spam by a provider or receiving service. When domain pool is enabled an email address will be generated ending in &#x60;@mailslurp.{world,info,xyz,...}&#x60; . This means a TLD is randomly selecting from a list of &#x60;.biz&#x60;, &#x60;.info&#x60;, &#x60;.xyz&#x60; etc to add variance to the generated email addresses. When null or false MailSlurp uses the default behavior of &#x60;@mailslurp.com&#x60; or custom email address provided by the emailAddress field. Note this feature is only available for &#x60;HTTP&#x60; inbox types. (optional)
     - parameter favourite: (query) Is the inbox a favorite. Marking an inbox as a favorite is typically done in the dashboard for quick access or filtering (optional)
     - parameter expiresAt: (query) Optional inbox expiration date. If null then this inbox is permanent and the emails in it won&#39;t be deleted. If an expiration date is provided or is required by your plan the inbox will be closed when the expiration time is reached. Expired inboxes still contain their emails but can no longer send or receive emails. An ExpiredInboxRecord is created when an inbox and the email address and inbox ID are recorded. The expiresAt property is a timestamp string in ISO DateTime Format yyyy-MM-dd&#39;T&#39;HH:mm:ss.SSSXXX. (optional)
     - parameter expiresIn: (query) Number of milliseconds that inbox should exist for (optional)
     - parameter allowTeamAccess: (query) DEPRECATED (team access is always true). Grant team access to this inbox and the emails that belong to it for team members of your organization. (optional)
     - parameter inboxType: (query) HTTP (default) or SMTP inbox type. HTTP inboxes are default and best solution for most cases. SMTP inboxes are more reliable for public inbound email consumption (but do not support sending emails). When using custom domains the domain type must match the inbox type. HTTP inboxes are processed by AWS SES while SMTP inboxes use a custom mail server running at &#x60;mx.mailslurp.com&#x60;. (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxDto>
     */
    open class func createInbox( emailAddress: String? = nil,  tags: [String]? = nil,  name: String? = nil,  _description: String? = nil,  useDomainPool: Bool? = nil,  favourite: Bool? = nil,  expiresAt: Date? = nil,  expiresIn: Int64? = nil,  allowTeamAccess: Bool? = nil,  inboxType: InboxType_createInbox? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxDto> {
        let deferred = Promise<InboxDto>.pending()
        createInboxWithRequestBuilder(emailAddress: emailAddress, tags: tags, name: name, _description: _description, useDomainPool: useDomainPool, favourite: favourite, expiresAt: expiresAt, expiresIn: expiresIn, allowTeamAccess: allowTeamAccess, inboxType: inboxType).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Create an inbox email address. An inbox has a real email address and can send and receive emails. Inboxes can be either `SMTP` or `HTTP` inboxes.
     - POST /inboxes
     - Create a new inbox and with a randomized email address to send and receive from. Pass emailAddress parameter if you wish to use a specific email address. Creating an inbox is required before sending or receiving emails. If writing tests it is recommended that you create a new inbox during each test method so that it is unique and empty. 
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter emailAddress: (query) A custom email address to use with the inbox. Defaults to null. When null MailSlurp will assign a random email address to the inbox such as &#x60;123@mailslurp.com&#x60;. If you use the &#x60;useDomainPool&#x60; option when the email address is null it will generate an email address with a more varied domain ending such as &#x60;123@mailslurp.info&#x60; or &#x60;123@mailslurp.biz&#x60;. When a custom email address is provided the address is split into a domain and the domain is queried against your user. If you have created the domain in the MailSlurp dashboard and verified it you can use any email address that ends with the domain. Note domain types must match the inbox type - so &#x60;SMTP&#x60; inboxes will only work with &#x60;SMTP&#x60; type domains. Avoid &#x60;SMTP&#x60; inboxes if you need to send emails as they can only receive. Send an email to this address and the inbox will receive and store it for you. To retrieve the email use the Inbox and Email Controller endpoints with the inbox ID. (optional)
     - parameter tags: (query) Tags that inbox has been tagged with. Tags can be added to inboxes to group different inboxes within an account. You can also search for inboxes by tag in the dashboard UI. (optional)
     - parameter name: (query) Optional name of the inbox. Displayed in the dashboard for easier search and used as the sender name when sending emails. (optional)
     - parameter _description: (query) Optional description of the inbox for labelling purposes. Is shown in the dashboard and can be used with (optional)
     - parameter useDomainPool: (query) Use the MailSlurp domain name pool with this inbox when creating the email address. Defaults to null. If enabled the inbox will be an email address with a domain randomly chosen from a list of the MailSlurp domains. This is useful when the default &#x60;@mailslurp.com&#x60; email addresses used with inboxes are blocked or considered spam by a provider or receiving service. When domain pool is enabled an email address will be generated ending in &#x60;@mailslurp.{world,info,xyz,...}&#x60; . This means a TLD is randomly selecting from a list of &#x60;.biz&#x60;, &#x60;.info&#x60;, &#x60;.xyz&#x60; etc to add variance to the generated email addresses. When null or false MailSlurp uses the default behavior of &#x60;@mailslurp.com&#x60; or custom email address provided by the emailAddress field. Note this feature is only available for &#x60;HTTP&#x60; inbox types. (optional)
     - parameter favourite: (query) Is the inbox a favorite. Marking an inbox as a favorite is typically done in the dashboard for quick access or filtering (optional)
     - parameter expiresAt: (query) Optional inbox expiration date. If null then this inbox is permanent and the emails in it won&#39;t be deleted. If an expiration date is provided or is required by your plan the inbox will be closed when the expiration time is reached. Expired inboxes still contain their emails but can no longer send or receive emails. An ExpiredInboxRecord is created when an inbox and the email address and inbox ID are recorded. The expiresAt property is a timestamp string in ISO DateTime Format yyyy-MM-dd&#39;T&#39;HH:mm:ss.SSSXXX. (optional)
     - parameter expiresIn: (query) Number of milliseconds that inbox should exist for (optional)
     - parameter allowTeamAccess: (query) DEPRECATED (team access is always true). Grant team access to this inbox and the emails that belong to it for team members of your organization. (optional)
     - parameter inboxType: (query) HTTP (default) or SMTP inbox type. HTTP inboxes are default and best solution for most cases. SMTP inboxes are more reliable for public inbound email consumption (but do not support sending emails). When using custom domains the domain type must match the inbox type. HTTP inboxes are processed by AWS SES while SMTP inboxes use a custom mail server running at &#x60;mx.mailslurp.com&#x60;. (optional)
     - returns: RequestBuilder<InboxDto> 
     */
    open class func createInboxWithRequestBuilder(emailAddress: String? = nil, tags: [String]? = nil, name: String? = nil, _description: String? = nil, useDomainPool: Bool? = nil, favourite: Bool? = nil, expiresAt: Date? = nil, expiresIn: Int64? = nil, allowTeamAccess: Bool? = nil, inboxType: InboxType_createInbox? = nil) -> RequestBuilder<InboxDto> {
        let path = "/inboxes"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "emailAddress": emailAddress?.encodeToJSON(),
            "tags": tags?.encodeToJSON(),
            "name": name?.encodeToJSON(),
            "description": _description?.encodeToJSON(),
            "useDomainPool": useDomainPool?.encodeToJSON(),
            "favourite": favourite?.encodeToJSON(),
            "expiresAt": expiresAt?.encodeToJSON(),
            "expiresIn": expiresIn?.encodeToJSON(),
            "allowTeamAccess": allowTeamAccess?.encodeToJSON(),
            "inboxType": inboxType?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Create an inbox ruleset
     
     - parameter inboxId: (path) inboxId 
     - parameter createInboxRulesetOptions: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxRulesetDto>
     */
    open class func createInboxRuleset( inboxId: UUID,  createInboxRulesetOptions: CreateInboxRulesetOptions, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxRulesetDto> {
        let deferred = Promise<InboxRulesetDto>.pending()
        createInboxRulesetWithRequestBuilder(inboxId: inboxId, createInboxRulesetOptions: createInboxRulesetOptions).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Create an inbox ruleset
     - POST /inboxes/{inboxId}/rulesets
     - Create a new inbox rule for forwarding, blocking, and allowing emails when sending and receiving
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) inboxId 
     - parameter createInboxRulesetOptions: (body)  
     - returns: RequestBuilder<InboxRulesetDto> 
     */
    open class func createInboxRulesetWithRequestBuilder(inboxId: UUID, createInboxRulesetOptions: CreateInboxRulesetOptions) -> RequestBuilder<InboxRulesetDto> {
        var path = "/inboxes/{inboxId}/rulesets"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createInboxRulesetOptions)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxRulesetDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Create an inbox with default options. Uses MailSlurp domain pool address and is private.
     
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxDto>
     */
    open class func createInboxWithDefaults(apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxDto> {
        let deferred = Promise<InboxDto>.pending()
        createInboxWithDefaultsWithRequestBuilder().execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Create an inbox with default options. Uses MailSlurp domain pool address and is private.
     - POST /inboxes/withDefaults
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - returns: RequestBuilder<InboxDto> 
     */
    open class func createInboxWithDefaultsWithRequestBuilder() -> RequestBuilder<InboxDto> {
        let path = "/inboxes/withDefaults"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Create an inbox with options. Extended options for inbox creation.
     
     - parameter createInboxDto: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxDto>
     */
    open class func createInboxWithOptions( createInboxDto: CreateInboxDto, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxDto> {
        let deferred = Promise<InboxDto>.pending()
        createInboxWithOptionsWithRequestBuilder(createInboxDto: createInboxDto).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Create an inbox with options. Extended options for inbox creation.
     - POST /inboxes/withOptions
     - Additional endpoint that allows inbox creation with request body options. Can be more flexible that other methods for some clients.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter createInboxDto: (body)  
     - returns: RequestBuilder<InboxDto> 
     */
    open class func createInboxWithOptionsWithRequestBuilder(createInboxDto: CreateInboxDto) -> RequestBuilder<InboxDto> {
        let path = "/inboxes/withOptions"
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createInboxDto)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Delete all inboxes
     
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<Void>
     */
    open class func deleteAllInboxes(apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<Void> {
        let deferred = Promise<Void>.pending()
        deleteAllInboxesWithRequestBuilder().execute(apiResponseQueue) { result -> Void in
            switch result {
            case .success:
                deferred.resolver.fulfill(())
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Delete all inboxes
     - DELETE /inboxes
     - Permanently delete all inboxes and associated email addresses. This will also delete all emails within the inboxes. Be careful as inboxes cannot be recovered once deleted. Note: deleting inboxes will not impact your usage limits. Monthly inbox creation limits are based on how many inboxes were created in the last 30 days, not how many inboxes you currently have.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - returns: RequestBuilder<Void> 
     */
    open class func deleteAllInboxesWithRequestBuilder() -> RequestBuilder<Void> {
        let path = "/inboxes"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<Void>.Type = mailslurpAPI.requestBuilderFactory.getNonDecodableBuilder()

        return requestBuilder.init(method: "DELETE", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Delete inbox
     
     - parameter inboxId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<Void>
     */
    open class func deleteInbox( inboxId: UUID, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<Void> {
        let deferred = Promise<Void>.pending()
        deleteInboxWithRequestBuilder(inboxId: inboxId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case .success:
                deferred.resolver.fulfill(())
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Delete inbox
     - DELETE /inboxes/{inboxId}
     - Permanently delete an inbox and associated email address as well as all emails within the given inbox. This action cannot be undone. Note: deleting an inbox will not affect your account usage. Monthly inbox usage is based on how many inboxes you create within 30 days, not how many exist at time of request.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - returns: RequestBuilder<Void> 
     */
    open class func deleteInboxWithRequestBuilder(inboxId: UUID) -> RequestBuilder<Void> {
        var path = "/inboxes/{inboxId}"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<Void>.Type = mailslurpAPI.requestBuilderFactory.getNonDecodableBuilder()

        return requestBuilder.init(method: "DELETE", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Does inbox exist
     
     - parameter emailAddress: (query) Email address 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxExistsDto>
     */
    open class func doesInboxExist( emailAddress: String, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxExistsDto> {
        let deferred = Promise<InboxExistsDto>.pending()
        doesInboxExistWithRequestBuilder(emailAddress: emailAddress).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Does inbox exist
     - GET /inboxes/exists
     - Check if inboxes exist by email address. Useful if you are sending emails to mailslurp addresses
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter emailAddress: (query) Email address 
     - returns: RequestBuilder<InboxExistsDto> 
     */
    open class func doesInboxExistWithRequestBuilder(emailAddress: String) -> RequestBuilder<InboxExistsDto> {
        let path = "/inboxes/exists"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "emailAddress": emailAddress.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxExistsDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Remove expired inboxes
     
     - parameter before: (query) Optional expired at before flag to flush expired inboxes that have expired before the given time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<FlushExpiredInboxesResult>
     */
    open class func flushExpired( before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<FlushExpiredInboxesResult> {
        let deferred = Promise<FlushExpiredInboxesResult>.pending()
        flushExpiredWithRequestBuilder(before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Remove expired inboxes
     - DELETE /inboxes/expired
     - Remove any expired inboxes for your account (instead of waiting for scheduled removal on server)
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter before: (query) Optional expired at before flag to flush expired inboxes that have expired before the given time (optional)
     - returns: RequestBuilder<FlushExpiredInboxesResult> 
     */
    open class func flushExpiredWithRequestBuilder(before: Date? = nil) -> RequestBuilder<FlushExpiredInboxesResult> {
        let path = "/inboxes/expired"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<FlushExpiredInboxesResult>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "DELETE", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_getAllInboxes: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     List All Inboxes Paginated
     
     - parameter page: (query) Optional page index in list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter favourite: (query) Optionally filter results for favourites only (optional, default to false)
     - parameter search: (query) Optionally filter by search words partial matching ID, tags, name, and email address (optional)
     - parameter tag: (query) Optionally filter by tags. Will return inboxes that include given tags (optional)
     - parameter teamAccess: (query) DEPRECATED. Optionally filter by team access. (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<PageInboxProjection>
     */
    open class func getAllInboxes( page: Int? = nil,  size: Int? = nil,  sort: Sort_getAllInboxes? = nil,  favourite: Bool? = nil,  search: String? = nil,  tag: String? = nil,  teamAccess: Bool? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<PageInboxProjection> {
        let deferred = Promise<PageInboxProjection>.pending()
        getAllInboxesWithRequestBuilder(page: page, size: size, sort: sort, favourite: favourite, search: search, tag: tag, teamAccess: teamAccess, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     List All Inboxes Paginated
     - GET /inboxes/paginated
     - List inboxes in paginated form. The results are available on the `content` property of the returned object. This method allows for page index (zero based), page size (how many results to return), and a sort direction (based on createdAt time). You Can also filter by whether an inbox is favorited or use email address pattern. This method is the recommended way to query inboxes. The alternative `getInboxes` method returns a full list of inboxes but is limited to 100 results.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter page: (query) Optional page index in list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter favourite: (query) Optionally filter results for favourites only (optional, default to false)
     - parameter search: (query) Optionally filter by search words partial matching ID, tags, name, and email address (optional)
     - parameter tag: (query) Optionally filter by tags. Will return inboxes that include given tags (optional)
     - parameter teamAccess: (query) DEPRECATED. Optionally filter by team access. (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - returns: RequestBuilder<PageInboxProjection> 
     */
    open class func getAllInboxesWithRequestBuilder(page: Int? = nil, size: Int? = nil, sort: Sort_getAllInboxes? = nil, favourite: Bool? = nil, search: String? = nil, tag: String? = nil, teamAccess: Bool? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<PageInboxProjection> {
        let path = "/inboxes/paginated"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "page": page?.encodeToJSON(),
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "favourite": favourite?.encodeToJSON(),
            "search": search?.encodeToJSON(),
            "tag": tag?.encodeToJSON(),
            "teamAccess": teamAccess?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<PageInboxProjection>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_getEmails: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     Get emails in an Inbox. This method is not idempotent as it allows retries and waits if you want certain conditions to be met before returning. For simple listing and sorting of known emails use the email controller instead.
     
     - parameter inboxId: (path) Id of inbox that emails belongs to 
     - parameter size: (query) Alias for limit. Assessed first before assessing any passed limit. (optional)
     - parameter limit: (query) Limit the result set, ordered by received date time sort direction. Maximum 100. For more listing options see the email controller (optional)
     - parameter sort: (query) Sort the results by received date and direction ASC or DESC (optional)
     - parameter retryTimeout: (query) Maximum milliseconds to spend retrying inbox database until minCount emails are returned (optional)
     - parameter delayTimeout: (query)  (optional)
     - parameter minCount: (query) Minimum acceptable email count. Will cause request to hang (and retry) until minCount is satisfied or retryTimeout is reached. (optional)
     - parameter unreadOnly: (query)  (optional)
     - parameter before: (query) Exclude emails received after this ISO 8601 date time (optional)
     - parameter since: (query) Exclude emails received before this ISO 8601 date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<[EmailPreview]>
     */
    open class func getEmails( inboxId: UUID,  size: Int? = nil,  limit: Int? = nil,  sort: Sort_getEmails? = nil,  retryTimeout: Int64? = nil,  delayTimeout: Int64? = nil,  minCount: Int64? = nil,  unreadOnly: Bool? = nil,  before: Date? = nil,  since: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<[EmailPreview]> {
        let deferred = Promise<[EmailPreview]>.pending()
        getEmailsWithRequestBuilder(inboxId: inboxId, size: size, limit: limit, sort: sort, retryTimeout: retryTimeout, delayTimeout: delayTimeout, minCount: minCount, unreadOnly: unreadOnly, before: before, since: since).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get emails in an Inbox. This method is not idempotent as it allows retries and waits if you want certain conditions to be met before returning. For simple listing and sorting of known emails use the email controller instead.
     - GET /inboxes/{inboxId}/emails
     - List emails that an inbox has received. Only emails that are sent to the inbox's email address will appear in the inbox. It may take several seconds for any email you send to an inbox's email address to appear in the inbox. To make this endpoint wait for a minimum number of emails use the `minCount` parameter. The server will retry the inbox database until the `minCount` is satisfied or the `retryTimeout` is reached
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) Id of inbox that emails belongs to 
     - parameter size: (query) Alias for limit. Assessed first before assessing any passed limit. (optional)
     - parameter limit: (query) Limit the result set, ordered by received date time sort direction. Maximum 100. For more listing options see the email controller (optional)
     - parameter sort: (query) Sort the results by received date and direction ASC or DESC (optional)
     - parameter retryTimeout: (query) Maximum milliseconds to spend retrying inbox database until minCount emails are returned (optional)
     - parameter delayTimeout: (query)  (optional)
     - parameter minCount: (query) Minimum acceptable email count. Will cause request to hang (and retry) until minCount is satisfied or retryTimeout is reached. (optional)
     - parameter unreadOnly: (query)  (optional)
     - parameter before: (query) Exclude emails received after this ISO 8601 date time (optional)
     - parameter since: (query) Exclude emails received before this ISO 8601 date time (optional)
     - returns: RequestBuilder<[EmailPreview]> 
     */
    open class func getEmailsWithRequestBuilder(inboxId: UUID, size: Int? = nil, limit: Int? = nil, sort: Sort_getEmails? = nil, retryTimeout: Int64? = nil, delayTimeout: Int64? = nil, minCount: Int64? = nil, unreadOnly: Bool? = nil, before: Date? = nil, since: Date? = nil) -> RequestBuilder<[EmailPreview]> {
        var path = "/inboxes/{inboxId}/emails"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "size": size?.encodeToJSON(),
            "limit": limit?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "retryTimeout": retryTimeout?.encodeToJSON(),
            "delayTimeout": delayTimeout?.encodeToJSON(),
            "minCount": minCount?.encodeToJSON(),
            "unreadOnly": unreadOnly?.encodeToJSON(),
            "before": before?.encodeToJSON(),
            "since": since?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<[EmailPreview]>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**

     - parameter inboxId: (query) Inbox ID (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<ImapSmtpAccessDetails>
     */
    open class func getImapSmtpAccess( inboxId: UUID? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<ImapSmtpAccessDetails> {
        let deferred = Promise<ImapSmtpAccessDetails>.pending()
        getImapSmtpAccessWithRequestBuilder(inboxId: inboxId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     - GET /inboxes/imap-smtp-access
     - Get IMAP and SMTP access usernames and passwords
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (query) Inbox ID (optional)
     - returns: RequestBuilder<ImapSmtpAccessDetails> 
     */
    open class func getImapSmtpAccessWithRequestBuilder(inboxId: UUID? = nil) -> RequestBuilder<ImapSmtpAccessDetails> {
        let path = "/inboxes/imap-smtp-access"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "inboxId": inboxId?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<ImapSmtpAccessDetails>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get Inbox. Returns properties of an inbox.
     
     - parameter inboxId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxDto>
     */
    open class func getInbox( inboxId: UUID, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxDto> {
        let deferred = Promise<InboxDto>.pending()
        getInboxWithRequestBuilder(inboxId: inboxId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get Inbox. Returns properties of an inbox.
     - GET /inboxes/{inboxId}
     - Returns an inbox's properties, including its email address and ID.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - returns: RequestBuilder<InboxDto> 
     */
    open class func getInboxWithRequestBuilder(inboxId: UUID) -> RequestBuilder<InboxDto> {
        var path = "/inboxes/{inboxId}"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Search for an inbox with the provided email address
     
     - parameter emailAddress: (query)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxByEmailAddressResult>
     */
    open class func getInboxByEmailAddress( emailAddress: String, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxByEmailAddressResult> {
        let deferred = Promise<InboxByEmailAddressResult>.pending()
        getInboxByEmailAddressWithRequestBuilder(emailAddress: emailAddress).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Search for an inbox with the provided email address
     - GET /inboxes/byEmailAddress
     - Get a inbox result by email address
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter emailAddress: (query)  
     - returns: RequestBuilder<InboxByEmailAddressResult> 
     */
    open class func getInboxByEmailAddressWithRequestBuilder(emailAddress: String) -> RequestBuilder<InboxByEmailAddressResult> {
        let path = "/inboxes/byEmailAddress"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "emailAddress": emailAddress.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxByEmailAddressResult>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get total inbox count
     
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<CountDto>
     */
    open class func getInboxCount(apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<CountDto> {
        let deferred = Promise<CountDto>.pending()
        getInboxCountWithRequestBuilder().execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get total inbox count
     - GET /inboxes/count
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - returns: RequestBuilder<CountDto> 
     */
    open class func getInboxCountWithRequestBuilder() -> RequestBuilder<CountDto> {
        let path = "/inboxes/count"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<CountDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get email count in inbox
     
     - parameter inboxId: (path) Id of inbox that emails belongs to 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<CountDto>
     */
    open class func getInboxEmailCount( inboxId: UUID, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<CountDto> {
        let deferred = Promise<CountDto>.pending()
        getInboxEmailCountWithRequestBuilder(inboxId: inboxId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get email count in inbox
     - GET /inboxes/{inboxId}/emails/count
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) Id of inbox that emails belongs to 
     - returns: RequestBuilder<CountDto> 
     */
    open class func getInboxEmailCountWithRequestBuilder(inboxId: UUID) -> RequestBuilder<CountDto> {
        var path = "/inboxes/{inboxId}/emails/count"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<CountDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_getInboxEmailsPaginated: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     Get inbox emails paginated
     
     - parameter inboxId: (path) Id of inbox that emails belongs to 
     - parameter page: (query) Optional page index in inbox emails list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox emails list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter since: (query) Optional filter by received after given date time (optional)
     - parameter before: (query) Optional filter by received before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<PageEmailPreview>
     */
    open class func getInboxEmailsPaginated( inboxId: UUID,  page: Int? = nil,  size: Int? = nil,  sort: Sort_getInboxEmailsPaginated? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<PageEmailPreview> {
        let deferred = Promise<PageEmailPreview>.pending()
        getInboxEmailsPaginatedWithRequestBuilder(inboxId: inboxId, page: page, size: size, sort: sort, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get inbox emails paginated
     - GET /inboxes/{inboxId}/emails/paginated
     - Get a paginated list of emails in an inbox. Does not hold connections open.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) Id of inbox that emails belongs to 
     - parameter page: (query) Optional page index in inbox emails list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox emails list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter since: (query) Optional filter by received after given date time (optional)
     - parameter before: (query) Optional filter by received before given date time (optional)
     - returns: RequestBuilder<PageEmailPreview> 
     */
    open class func getInboxEmailsPaginatedWithRequestBuilder(inboxId: UUID, page: Int? = nil, size: Int? = nil, sort: Sort_getInboxEmailsPaginated? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<PageEmailPreview> {
        var path = "/inboxes/{inboxId}/emails/paginated"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "page": page?.encodeToJSON(),
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<PageEmailPreview>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get all inbox IDs
     
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxIdsResult>
     */
    open class func getInboxIds(apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxIdsResult> {
        let deferred = Promise<InboxIdsResult>.pending()
        getInboxIdsWithRequestBuilder().execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get all inbox IDs
     - GET /inboxes/ids
     - Get list of inbox IDs
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - returns: RequestBuilder<InboxIdsResult> 
     */
    open class func getInboxIdsWithRequestBuilder() -> RequestBuilder<InboxIdsResult> {
        let path = "/inboxes/ids"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxIdsResult>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_getInboxSentEmails: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     Get Inbox Sent Emails
     
     - parameter inboxId: (path)  
     - parameter page: (query) Optional page index in inbox sent email list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox sent email list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional sent email search (optional)
     - parameter since: (query) Optional filter by sent after given date time (optional)
     - parameter before: (query) Optional filter by sent before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<PageSentEmailProjection>
     */
    open class func getInboxSentEmails( inboxId: UUID,  page: Int? = nil,  size: Int? = nil,  sort: Sort_getInboxSentEmails? = nil,  searchFilter: String? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<PageSentEmailProjection> {
        let deferred = Promise<PageSentEmailProjection>.pending()
        getInboxSentEmailsWithRequestBuilder(inboxId: inboxId, page: page, size: size, sort: sort, searchFilter: searchFilter, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get Inbox Sent Emails
     - GET /inboxes/{inboxId}/sent
     - Returns an inbox's sent email receipts. Call individual sent email endpoints for more details. Note for privacy reasons the full body of sent emails is never stored. An MD5 hash hex is available for comparison instead.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - parameter page: (query) Optional page index in inbox sent email list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox sent email list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional sent email search (optional)
     - parameter since: (query) Optional filter by sent after given date time (optional)
     - parameter before: (query) Optional filter by sent before given date time (optional)
     - returns: RequestBuilder<PageSentEmailProjection> 
     */
    open class func getInboxSentEmailsWithRequestBuilder(inboxId: UUID, page: Int? = nil, size: Int? = nil, sort: Sort_getInboxSentEmails? = nil, searchFilter: String? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<PageSentEmailProjection> {
        var path = "/inboxes/{inboxId}/sent"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "page": page?.encodeToJSON(),
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "searchFilter": searchFilter?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<PageSentEmailProjection>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get inbox tags
     
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<[String]>
     */
    open class func getInboxTags(apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<[String]> {
        let deferred = Promise<[String]>.pending()
        getInboxTagsWithRequestBuilder().execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get inbox tags
     - GET /inboxes/tags
     - Get all inbox tags
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - returns: RequestBuilder<[String]> 
     */
    open class func getInboxTagsWithRequestBuilder() -> RequestBuilder<[String]> {
        let path = "/inboxes/tags"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<[String]>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_getInboxes: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     List Inboxes and email addresses
     
     - parameter size: (query) Optional result size limit. Note an automatic limit of 100 results is applied. See the paginated &#x60;getAllEmails&#x60; for larger queries. (optional, default to 100)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<[InboxDto]>
     */
    @available(*, deprecated, message: "This operation is deprecated.")
    open class func getInboxes( size: Int? = nil,  sort: Sort_getInboxes? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<[InboxDto]> {
        let deferred = Promise<[InboxDto]>.pending()
        getInboxesWithRequestBuilder(size: size, sort: sort, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     List Inboxes and email addresses
     - GET /inboxes
     - List the inboxes you have created. Note use of the more advanced `getAllEmails` is recommended and allows paginated access using a limit and sort parameter.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter size: (query) Optional result size limit. Note an automatic limit of 100 results is applied. See the paginated &#x60;getAllEmails&#x60; for larger queries. (optional, default to 100)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - returns: RequestBuilder<[InboxDto]> 
     */
    @available(*, deprecated, message: "This operation is deprecated.")
    open class func getInboxesWithRequestBuilder(size: Int? = nil, sort: Sort_getInboxes? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<[InboxDto]> {
        let path = "/inboxes"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<[InboxDto]>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Get latest email in an inbox. Use `WaitForController` to get emails that may not have arrived yet.
     
     - parameter inboxId: (query) ID of the inbox you want to get the latest email from 
     - parameter timeoutMillis: (query) Timeout milliseconds to wait for latest email 
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<Email>
     */
    open class func getLatestEmailInInbox( inboxId: UUID,  timeoutMillis: Int64, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<Email> {
        let deferred = Promise<Email>.pending()
        getLatestEmailInInboxWithRequestBuilder(inboxId: inboxId, timeoutMillis: timeoutMillis).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Get latest email in an inbox. Use `WaitForController` to get emails that may not have arrived yet.
     - GET /inboxes/getLatestEmail
     - Get the newest email in an inbox or wait for one to arrive
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (query) ID of the inbox you want to get the latest email from 
     - parameter timeoutMillis: (query) Timeout milliseconds to wait for latest email 
     - returns: RequestBuilder<Email> 
     */
    open class func getLatestEmailInInboxWithRequestBuilder(inboxId: UUID, timeoutMillis: Int64) -> RequestBuilder<Email> {
        let path = "/inboxes/getLatestEmail"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "inboxId": inboxId.encodeToJSON(),
            "timeoutMillis": timeoutMillis.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<Email>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_getOrganizationInboxes: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     List Organization Inboxes Paginated
     
     - parameter page: (query) Optional page index in list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional search filter (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<PageOrganizationInboxProjection>
     */
    open class func getOrganizationInboxes( page: Int? = nil,  size: Int? = nil,  sort: Sort_getOrganizationInboxes? = nil,  searchFilter: String? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<PageOrganizationInboxProjection> {
        let deferred = Promise<PageOrganizationInboxProjection>.pending()
        getOrganizationInboxesWithRequestBuilder(page: page, size: size, sort: sort, searchFilter: searchFilter, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     List Organization Inboxes Paginated
     - GET /inboxes/organization
     - List organization inboxes in paginated form. These are inboxes created with `allowTeamAccess` flag enabled. Organization inboxes are `readOnly` for non-admin users. The results are available on the `content` property of the returned object. This method allows for page index (zero based), page size (how many results to return), and a sort direction (based on createdAt time). 
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter page: (query) Optional page index in list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional search filter (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - returns: RequestBuilder<PageOrganizationInboxProjection> 
     */
    open class func getOrganizationInboxesWithRequestBuilder(page: Int? = nil, size: Int? = nil, sort: Sort_getOrganizationInboxes? = nil, searchFilter: String? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<PageOrganizationInboxProjection> {
        let path = "/inboxes/organization"
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "page": page?.encodeToJSON(),
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "searchFilter": searchFilter?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<PageOrganizationInboxProjection>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_listInboxRulesets: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     List inbox rulesets
     
     - parameter inboxId: (path)  
     - parameter page: (query) Optional page index in inbox ruleset list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox ruleset list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional search filter (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<PageInboxRulesetDto>
     */
    open class func listInboxRulesets( inboxId: UUID,  page: Int? = nil,  size: Int? = nil,  sort: Sort_listInboxRulesets? = nil,  searchFilter: String? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<PageInboxRulesetDto> {
        let deferred = Promise<PageInboxRulesetDto>.pending()
        listInboxRulesetsWithRequestBuilder(inboxId: inboxId, page: page, size: size, sort: sort, searchFilter: searchFilter, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     List inbox rulesets
     - GET /inboxes/{inboxId}/rulesets
     - List all rulesets attached to an inbox
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - parameter page: (query) Optional page index in inbox ruleset list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox ruleset list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional search filter (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - returns: RequestBuilder<PageInboxRulesetDto> 
     */
    open class func listInboxRulesetsWithRequestBuilder(inboxId: UUID, page: Int? = nil, size: Int? = nil, sort: Sort_listInboxRulesets? = nil, searchFilter: String? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<PageInboxRulesetDto> {
        var path = "/inboxes/{inboxId}/rulesets"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "page": page?.encodeToJSON(),
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "searchFilter": searchFilter?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<PageInboxRulesetDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     * enum for parameter sort
     */
    public enum Sort_listInboxTrackingPixels: String, CaseIterable {
        case asc = "ASC"
        case desc = "DESC"
    }

    /**
     List inbox tracking pixels
     
     - parameter inboxId: (path)  
     - parameter page: (query) Optional page index in inbox tracking pixel list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox tracking pixel list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional search filter (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<PageTrackingPixelProjection>
     */
    open class func listInboxTrackingPixels( inboxId: UUID,  page: Int? = nil,  size: Int? = nil,  sort: Sort_listInboxTrackingPixels? = nil,  searchFilter: String? = nil,  since: Date? = nil,  before: Date? = nil, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<PageTrackingPixelProjection> {
        let deferred = Promise<PageTrackingPixelProjection>.pending()
        listInboxTrackingPixelsWithRequestBuilder(inboxId: inboxId, page: page, size: size, sort: sort, searchFilter: searchFilter, since: since, before: before).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     List inbox tracking pixels
     - GET /inboxes/{inboxId}/tracking-pixels
     - List all tracking pixels sent from an inbox
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - parameter page: (query) Optional page index in inbox tracking pixel list pagination (optional, default to 0)
     - parameter size: (query) Optional page size in inbox tracking pixel list pagination (optional, default to 20)
     - parameter sort: (query) Optional createdAt sort direction ASC or DESC (optional, default to .asc)
     - parameter searchFilter: (query) Optional search filter (optional)
     - parameter since: (query) Optional filter by created after given date time (optional)
     - parameter before: (query) Optional filter by created before given date time (optional)
     - returns: RequestBuilder<PageTrackingPixelProjection> 
     */
    open class func listInboxTrackingPixelsWithRequestBuilder(inboxId: UUID, page: Int? = nil, size: Int? = nil, sort: Sort_listInboxTrackingPixels? = nil, searchFilter: String? = nil, since: Date? = nil, before: Date? = nil) -> RequestBuilder<PageTrackingPixelProjection> {
        var path = "/inboxes/{inboxId}/tracking-pixels"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        var urlComponents = URLComponents(string: URLString)
        urlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "page": page?.encodeToJSON(),
            "size": size?.encodeToJSON(),
            "sort": sort?.encodeToJSON(),
            "searchFilter": searchFilter?.encodeToJSON(),
            "since": since?.encodeToJSON(),
            "before": before?.encodeToJSON(),
        ])

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<PageTrackingPixelProjection>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "GET", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Send Email
     
     - parameter inboxId: (path) ID of the inbox you want to send the email from 
     - parameter sendEmailOptions: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<Void>
     */
    open class func sendEmail( inboxId: UUID,  sendEmailOptions: SendEmailOptions, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<Void> {
        let deferred = Promise<Void>.pending()
        sendEmailWithRequestBuilder(inboxId: inboxId, sendEmailOptions: sendEmailOptions).execute(apiResponseQueue) { result -> Void in
            switch result {
            case .success:
                deferred.resolver.fulfill(())
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Send Email
     - POST /inboxes/{inboxId}
     - Send an email from an inbox's email address.  The request body should contain the `SendEmailOptions` that include recipients, attachments, body etc. See `SendEmailOptions` for all available properties. Note the `inboxId` refers to the inbox's id not the inbox's email address. See https://www.mailslurp.com/guides/ for more information on how to send emails. This method does not return a sent email entity due to legacy reasons. To send and get a sent email as returned response use the sister method `sendEmailAndConfirm`.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) ID of the inbox you want to send the email from 
     - parameter sendEmailOptions: (body)  
     - returns: RequestBuilder<Void> 
     */
    open class func sendEmailWithRequestBuilder(inboxId: UUID, sendEmailOptions: SendEmailOptions) -> RequestBuilder<Void> {
        var path = "/inboxes/{inboxId}"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: sendEmailOptions)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<Void>.Type = mailslurpAPI.requestBuilderFactory.getNonDecodableBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Send email and return sent confirmation
     
     - parameter inboxId: (path) ID of the inbox you want to send the email from 
     - parameter sendEmailOptions: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<SentEmailDto>
     */
    open class func sendEmailAndConfirm( inboxId: UUID,  sendEmailOptions: SendEmailOptions, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<SentEmailDto> {
        let deferred = Promise<SentEmailDto>.pending()
        sendEmailAndConfirmWithRequestBuilder(inboxId: inboxId, sendEmailOptions: sendEmailOptions).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Send email and return sent confirmation
     - POST /inboxes/{inboxId}/confirm
     - Sister method for standard `sendEmail` method with the benefit of returning a `SentEmail` entity confirming the successful sending of the email with a link to the sent object created for it.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) ID of the inbox you want to send the email from 
     - parameter sendEmailOptions: (body)  
     - returns: RequestBuilder<SentEmailDto> 
     */
    open class func sendEmailAndConfirmWithRequestBuilder(inboxId: UUID, sendEmailOptions: SendEmailOptions) -> RequestBuilder<SentEmailDto> {
        var path = "/inboxes/{inboxId}/confirm"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: sendEmailOptions)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<SentEmailDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Send email using an SMTP mail envelope and message body and return sent confirmation
     
     - parameter inboxId: (path) ID of the inbox you want to send the email from 
     - parameter sendSMTPEnvelopeOptions: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<SentEmailDto>
     */
    open class func sendSmtpEnvelope( inboxId: UUID,  sendSMTPEnvelopeOptions: SendSMTPEnvelopeOptions, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<SentEmailDto> {
        let deferred = Promise<SentEmailDto>.pending()
        sendSmtpEnvelopeWithRequestBuilder(inboxId: inboxId, sendSMTPEnvelopeOptions: sendSMTPEnvelopeOptions).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Send email using an SMTP mail envelope and message body and return sent confirmation
     - POST /inboxes/{inboxId}/smtp-envelope
     - Send email using an SMTP envelope containing RCPT TO, MAIL FROM, and a SMTP BODY.
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path) ID of the inbox you want to send the email from 
     - parameter sendSMTPEnvelopeOptions: (body)  
     - returns: RequestBuilder<SentEmailDto> 
     */
    open class func sendSmtpEnvelopeWithRequestBuilder(inboxId: UUID, sendSMTPEnvelopeOptions: SendSMTPEnvelopeOptions) -> RequestBuilder<SentEmailDto> {
        var path = "/inboxes/{inboxId}/smtp-envelope"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: sendSMTPEnvelopeOptions)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<SentEmailDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Send a test email to inbox
     
     - parameter inboxId: (path)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<Void>
     */
    open class func sendTestEmail( inboxId: UUID, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<Void> {
        let deferred = Promise<Void>.pending()
        sendTestEmailWithRequestBuilder(inboxId: inboxId).execute(apiResponseQueue) { result -> Void in
            switch result {
            case .success:
                deferred.resolver.fulfill(())
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Send a test email to inbox
     - POST /inboxes/{inboxId}/send-test-email
     - Send an inbox a test email to test email receiving is working
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - returns: RequestBuilder<Void> 
     */
    open class func sendTestEmailWithRequestBuilder(inboxId: UUID) -> RequestBuilder<Void> {
        var path = "/inboxes/{inboxId}/send-test-email"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters: [String: Any]? = nil

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<Void>.Type = mailslurpAPI.requestBuilderFactory.getNonDecodableBuilder()

        return requestBuilder.init(method: "POST", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Set inbox favourited state
     
     - parameter inboxId: (path)  
     - parameter setInboxFavouritedOptions: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxDto>
     */
    open class func setInboxFavourited( inboxId: UUID,  setInboxFavouritedOptions: SetInboxFavouritedOptions, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxDto> {
        let deferred = Promise<InboxDto>.pending()
        setInboxFavouritedWithRequestBuilder(inboxId: inboxId, setInboxFavouritedOptions: setInboxFavouritedOptions).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Set inbox favourited state
     - PUT /inboxes/{inboxId}/favourite
     - Set and return new favourite state for an inbox
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - parameter setInboxFavouritedOptions: (body)  
     - returns: RequestBuilder<InboxDto> 
     */
    open class func setInboxFavouritedWithRequestBuilder(inboxId: UUID, setInboxFavouritedOptions: SetInboxFavouritedOptions) -> RequestBuilder<InboxDto> {
        var path = "/inboxes/{inboxId}/favourite"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: setInboxFavouritedOptions)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "PUT", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

    /**
     Update Inbox. Change name and description. Email address is not editable.
     
     - parameter inboxId: (path)  
     - parameter updateInboxOptions: (body)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - returns: Promise<InboxDto>
     */
    open class func updateInbox( inboxId: UUID,  updateInboxOptions: UpdateInboxOptions, apiResponseQueue: DispatchQueue = mailslurpAPI.apiResponseQueue) -> Promise<InboxDto> {
        let deferred = Promise<InboxDto>.pending()
        updateInboxWithRequestBuilder(inboxId: inboxId, updateInboxOptions: updateInboxOptions).execute(apiResponseQueue) { result -> Void in
            switch result {
            case let .success(response):
                deferred.resolver.fulfill(response.body!)
            case let .failure(error):
                deferred.resolver.reject(error)
            }
        }
        return deferred.promise
    }

    /**
     Update Inbox. Change name and description. Email address is not editable.
     - PATCH /inboxes/{inboxId}
     - Update editable fields on an inbox
     - API Key:
       - type: apiKey x-api-key 
       - name: API_KEY
     - parameter inboxId: (path)  
     - parameter updateInboxOptions: (body)  
     - returns: RequestBuilder<InboxDto> 
     */
    open class func updateInboxWithRequestBuilder(inboxId: UUID, updateInboxOptions: UpdateInboxOptions) -> RequestBuilder<InboxDto> {
        var path = "/inboxes/{inboxId}"
        let inboxIdPreEscape = "\(APIHelper.mapValueToPathItem(inboxId))"
        let inboxIdPostEscape = inboxIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        path = path.replacingOccurrences(of: "{inboxId}", with: inboxIdPostEscape, options: .literal, range: nil)
        let URLString = mailslurpAPI.basePath + path
        let parameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateInboxOptions)

        let urlComponents = URLComponents(string: URLString)

        let nillableHeaders: [String: Any?] = [
            :
        ]

        let headerParameters = APIHelper.rejectNilHeaders(nillableHeaders)

        let requestBuilder: RequestBuilder<InboxDto>.Type = mailslurpAPI.requestBuilderFactory.getBuilder()

        return requestBuilder.init(method: "PATCH", URLString: (urlComponents?.string ?? URLString), parameters: parameters, headers: headerParameters)
    }

}
